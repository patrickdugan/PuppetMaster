# Runs

Runtime artifacts are written under `runs/` by default. For large test suites, set `PM_RUNS_DIR` to a path on a larger disk (for example, `D:\PupperMasterLogs`) to avoid filling the system drive.
